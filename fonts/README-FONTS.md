# Fonts — Status

ERLEDIGT: Titillium Web liegt lokal als ttf in `fonts/` (400/600/700/900),
verdrahtet über `fonts/titillium.css`. Lizenz: `fonts/OFL.txt`.
Italics + Leichtschnitte bleiben in `uploads/` (Blueprint nutzt sie nicht).

Vor Go-Live optional: ttf → woff2 konvertieren (~60 % kleiner), Dateinamen
in `titillium.css` anpassen. Kein Google-Fonts-CDN (DSGVO).
